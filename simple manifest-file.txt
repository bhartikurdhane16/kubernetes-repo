# guidelines to create simple manifest file to create pods in minikube
# plug-in required: autogit & kubernetes

steps:
1. 

1. create .ymal file to create pod(pod-->container--->application).
2. try to acces application within minikube cluster---accessable
3. try to access application outside the cluster ----not accessable.
4. to access application from outside the cluster need to espose service, so to expose service create another .yml file
